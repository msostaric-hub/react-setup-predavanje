import { css } from '@emotion/react';

const global = css`
  body {
    margin: 0;
    background-color: #fafafafa;
    font-family: arial;
    font-weight: bold;
  }
`;

const styles = {
  global,
};

export default styles;
